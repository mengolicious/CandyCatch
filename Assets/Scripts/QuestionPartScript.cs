using UnityEngine;
using System.Collections;

public class QuestionPartScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	public void DestroyThisObject(){

		Destroy (this.gameObject);
	}
}
